<Window x:Class="WpfMvvmApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WpfMvvmApp.ViewModels"
        xmlns:conv="clr-namespace:WpfMvvmApp.Converters"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="820" Width="1100"
        MinHeight="600" MinWidth="900">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <conv:NullToVisibilityConverter x:Key="NullToVisibility" />
    </Window.Resources>

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="12" />
            <RowDefinition Height="3*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#FFCAD0DA" BorderThickness="1" CornerRadius="8" Background="#FFF7F9FC" Padding="10">
            <Grid>
                <Image Source="{Binding FormImageUri}" Stretch="Uniform" />
                <TextBlock Text="Provide the scanned form image at Data/sample-dental-form.png"
                           Foreground="#99000000"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontStyle="Italic"
                           Visibility="{Binding FormImageUri, Converter={StaticResource NullToVisibility}, ConverterParameter=invert}" />
            </Grid>
        </Border>

        <Border Grid.Row="2" BorderBrush="#FFCAD0DA" BorderThickness="1" CornerRadius="8" Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Sections}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,16">
                                <TextBlock Text="{Binding Header}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8" />
                                <ItemsControl ItemsSource="{Binding Fields}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="{Binding DataContext.Columns, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="0,0,12,10" Padding="8" BorderBrush="#FFE0E3E8" BorderThickness="1" CornerRadius="6">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Label}" FontWeight="Medium" Margin="0,0,0,4" />
                                                    <TextBox Text="{Binding TextValue, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding IsText, Converter={StaticResource BoolToVisibility}}" />
                                                    <DatePicker SelectedDate="{Binding DateValue}" Visibility="{Binding IsDate, Converter={StaticResource BoolToVisibility}}" />
                                                    <TextBox Text="{Binding TextValue, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="70" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Visibility="{Binding IsMultiline, Converter={StaticResource BoolToVisibility}}" />
                                                    <CheckBox Content="Confirmed" IsChecked="{Binding BoolValue}" Visibility="{Binding IsCheckbox, Converter={StaticResource BoolToVisibility}}" />
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
